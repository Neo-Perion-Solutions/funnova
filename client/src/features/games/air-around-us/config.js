export const airAroundUsConfig = {
  title: 'Air Around Us',
  totalRounds: 15,
  timePerRound: 0, // dynamic per level, GameEngine overrides via action.payload in NEXT_LEVEL
  pointsPerCorrect: 10,
};
